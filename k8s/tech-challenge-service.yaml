apiVersion: v1
kind: Service
metadata:
  name: tech-challenge-service
  namespace: tech-challenge
spec:
  type: NodePort
  selector:
    app: tech-challenge
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
      nodePort: 31000
